name: Terraform & Lambda Deployment

on:
  push:
    branches:
      - main

jobs:
  deploy:
    name: Deploy Infrastructure and Lambda
    runs-on: ubuntu-latest

    env:
      AWS_REGION: us-east-1  # 🔁 Change this if needed

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: 1.5.7

      - name: Terraform Init
        run: terraform init

      - name: Terraform Plan
        run: terraform plan

      - name: Terraform Apply
        run: terraform apply -auto-approve
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

      - name: Zip Lambda function
        run: |
          cd lambda
          zip -r ../lambda.zip .

      - name: Upload Lambda Code
        run: |
          aws lambda update-function-code \
            --function-name my_lambda_function \
            --zip-file fileb://lambda.zip \
            --region $AWS_REGION
        env:
          AWS_ACCESS_KEY_ID: ${{ AKIA2FYYIRQP7YI7EQ2O }}
          AWS_SECRET_ACCESS_KEY: ${{ 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 }}
